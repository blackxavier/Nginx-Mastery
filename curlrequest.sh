curl http://127.0.0.1/admin/login &
curl http://127.0.0.1/admin/login &
curl http://127.0.0.1/admin/login &
curl http://127.0.0.1/admin/login &
curl http://127.0.0.1/admin/login &
curl http://127.0.0.1/admin/login &
curl http://127.0.0.1/admin/login &

# the & at the end of the command will make the command run in parallel
# while the && will make the command run sequentially
# you can use a test page for this instead of using the django admin login